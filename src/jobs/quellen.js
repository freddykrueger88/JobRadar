'use strict';

/**
 * Job-Portal-Adapter.
 * Jeder Eintrag muss { id, fetch({ suchbegriff, ort, umkreis }) } exportieren.
 * fetch() gibt ein Promise<Array> zurück.
 *
 * TODO: echte Adapter für Stepstone, Indeed, etc. hier eintragen.
 */

module.exports = [];
